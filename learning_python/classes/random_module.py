# The module random contains functions that generate random numbers in a
# variety of ways. The function randint() returns an integer in the range 
# you provide. 

from random import randint
x = randint(1,6)
print(x)