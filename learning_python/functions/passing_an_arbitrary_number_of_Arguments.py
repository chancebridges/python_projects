# sometimes you won't know ahead of times how many arguments a function needs to accept

# python allows a function to collect an arbitrary number of arguments from the calling statement

# reference pizza.py