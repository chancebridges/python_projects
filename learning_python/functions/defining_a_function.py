
def greet_user():
    """Display a simple greeting."""
    print("Hello!")

greet_user()

# this example shows the simplest structure of a function.
# line 2 uses the keyword def to inform python that you are defining a function
# This is a function definition, which tells python the name of the function
# and what kind of information the function needs to do its job

# The parentheses hold the information, however the able function needs no 
# information to do its job, so it is empty.

# any indented lines that follow def make up the body of the function

# the text at line 3 are called a docstring, which describes what the function does.

# when you want to use this function, you call it. a function
#  call tells python to execute the code in the function

# to call a function, you write the name of the function followed by any necessary
# information in parentheses